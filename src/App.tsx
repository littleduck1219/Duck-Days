import React from "react";

import { Router } from "Components/Router";
import Layout from "Components/Layout";

const App = () => {
	return (
		<Layout>
			<Router />
		</Layout>
	);
};

export default App;
